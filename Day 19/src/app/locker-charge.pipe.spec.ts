import { LockerChargePipe } from './locker-charge.pipe';

describe('LockerChargePipe', () => {
  it('create an instance', () => {
    const pipe = new LockerChargePipe();
    expect(pipe).toBeTruthy();
  });
});
